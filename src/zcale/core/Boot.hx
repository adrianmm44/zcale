/////////////////////////////////////////////////////////////////////////////////////////////////////////

package zcale.core;

#if flash
	typedef Boot = zcale.core.boot.targets.FlashBoot;
#elseif js
	typedef Boot = zcale.core.boot.targets.JsBoot;
#elseif ( neko || php )
	typedef Boot = zcale.core.boot.targets.ServerBoot;
#else
	#error "unknown target";
#end

/////////////////////////////////////////////////////////////////////////////////////////////////////////