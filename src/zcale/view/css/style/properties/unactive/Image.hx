
class Image
{	
	public function imageOrientation( v : String ) : Style // Specifies a rotation in the right or clockwise direction that a user agent applies to an image (This property is likely going to be deprecated and its functionality moved to HTML) 3
	{
		return this;
	}
	
	public function imageRendering( v : String ) : Style // Gives a hint to the browser about what aspects of an image are most important to preserve when the image is scaled 3
	{
		return this;
	}
	
	public function imageResolution( v : String ) : Style // Specifies the intrinsic resolution of all raster images used in/on the element 3
	{
		return this;
	}
}