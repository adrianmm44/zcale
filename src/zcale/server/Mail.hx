/////////////////////////////////////////////////////////////////////////////////////////////////////////

package hxf.core.system;

import hxf.core.Registry;

class Mail 
#if flash
	extends hxf.core.system.mail.targets.ClientMail
#elseif js
	extends hxf.core.system.mail.targets.ClientMail
#elseif ( php || server || system || macro )
	extends hxf.core.system.mail.targets.PhpMail
#else
	#error "target unknown"
#end
#if debug
	implements hxf.core.system.mail.interfaces.IMail
#end
{
	/////////////////////////////////////////////////////////////////////////////////////////////////////

	public function new( reg : Registry )
	{
		super( this, reg );
	}
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////