/////////////////////////////////////////////////////////////////////////////////////////////////////////

package hxf.core.registry;

import hxf.core.Registry;

class Location 
#if flash
	extends hxf.core.registry.location.targets.FlashLocation
#elseif jquery
	extends hxf.core.registry.location.targets.JsLocation
#elseif js
	extends hxf.core.registry.location.targets.JsLocation
#elseif ( neko || php || macro )
	extends hxf.core.registry.location.targets.ServerLocation
#else
	#error "target unknown"
#end
#if debug
	implements hxf.core.registry.location.interfaces.ILocation
#end
{
	/////////////////////////////////////////////////////////////////////////////////////////////////////
	
	public function new( reg : Registry )
	{
		super( reg );
	}
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////